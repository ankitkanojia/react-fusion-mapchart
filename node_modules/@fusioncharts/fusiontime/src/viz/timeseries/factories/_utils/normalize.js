import isValidXAxisConfig from"./is-valid-config";import toXAxisConfig from"./to-config";import isArray from"@fusioncharts/utils/src/type/is-array";import isString from"@fusioncharts/utils/src/type/is-string";export default function(config){if(config===void 0){config=[]}if(isArray(config)&&config.length>0){return config.filter(function(cfg){return isString(cfg)||isValidXAxisConfig(cfg)}).map(function(cfg){return toXAxisConfig(cfg)})}else if(isString(config)||isValidXAxisConfig(config)){return[toXAxisConfig(config)]}return[]}