import ColorRange from"@fusioncharts/core/src/color-utils/colorrange";function _createColorRangeManager(chart){var manager=chart.getChildren("colorManager");if(!chart.getFromEnv("dataSource").colorrange){manager&&(manager[0].config.legendItemIds=[]);chart.deleteFromEnv("colorManager");return}if(manager){chart.addToEnv("colorManager",manager[0]);manager[0].configure();return}manager=new ColorRange;chart.attachChild(manager,"colorManager");chart.addToEnv("colorManager",manager);manager.configure()}export default function(chart){_createColorRangeManager(chart)}